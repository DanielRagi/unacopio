# Normaliza finales de línea: el repo guarda LF y cada quien trabaja con lo que
# le toque su sistema. Evita diffs de archivos enteros entre Windows y Linux.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.svg text eol=lf
