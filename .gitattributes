# Los CSV que produce el pilotaje llevan CRLF y BOM a propósito: es lo que hace
# que Excel en Windows los abra con las tildes bien. Si git normaliza los saltos
# de línea, eso se pierde en el checkout.
*.csv -text
